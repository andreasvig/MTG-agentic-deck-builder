{
  "name": "mtg-agentic-deck-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Commander deck builder development workspace",
  "scripts": {
    "setup": "uv sync --project backend --all-groups && npm install --prefix frontend",
    "catalog:sync": "uv run --project backend python -m mtg_deck_builder.catalog_sync",
    "tagger:sync": "uv run --project backend python -m mtg_deck_builder.tagger_sync",
    "dev": "node --env-file-if-exists=.env scripts/dev.mjs",
    "test": "uv --directory backend run pytest && npm test --prefix frontend && node scripts/smoke.mjs",
    "test:e2e": "playwright test",
    "test:smoke": "node scripts/smoke.mjs",
    "build": "npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
